// Temporary failing test to demonstrate the workflow
describe('Failing Test Demo', () => {
  test('should fail intentionally', () => {
    expect(1 + 1).toBe(3); // This will fail
  });

  test('should pass', () => {
    expect(2 + 2).toBe(4); // This will pass
  });
});
